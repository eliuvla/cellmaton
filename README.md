# formato de autómata:
Debes de crear una carpeta en "examples" con el un nombre y un archivo .py del mismo tipo, ejemplo "ejemplo_1" y dentro de esa carpeta el archivo debe de llamarse "ejemplo_1.py"
donde sólo tendrá la función rule 